[package]
name = "state-m-test"
version = "0.1.0"
edition = "2024"

[dependencies]
anyhow = "1.0"
state-m = "0.5"
tokio = { version = "1.52", features = ["macros", "rt", "sync"] }
tokio-util = "0.7"
tracing = "0.1"

[features]
default = []
